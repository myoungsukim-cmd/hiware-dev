# =============================================================================
# Docker MySQL 컨테이너 전용
# cp docker/mysql/.env.example docker/mysql/.env && vi docker/mysql/.env
#
# ★ 루트 .env 의 DB_CATALOG / DB_USERNAME / DB_PASSWORD / DB_PORT 와 동일하게
# =============================================================================

# DB
DB_PORT=3306
DB_CATALOG=slack_hiware_approval
DB_USERNAME=slack_hiware
DB_PASSWORD=change-me
DB_ROOT_PASSWORD=change-root-password
